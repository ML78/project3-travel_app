<!-- <p id="notice"><%= notice %></p> -->

<h1><%= "#{@user.name}'s Page" %></h1>

<div>
  <!-- <p>
    <strong>Name:</strong>
    <%= @user.name %>
  </p> -->

  <% unless @user.image.empty? %>
    <%= image_tag @user.image, :alt => @user.name, :id => 'profile-img' %>
  <% end %>


  <% if @current_user.present? %>
  <%= link_to "Add a new itinerary", new_itinerary_path, :class => 'btn btn-default btn-info' %>
  <%= link_to "Add a new activity", new_activity_path, :class => 'btn btn-default btn-info' %>
  <% end %>

  <% unless @user.itineraries.empty? %>
    <h2>Itineraries</h2>
    <% @user.itineraries.each do |itinerary| %>
      <ul>
        <li>
          <%= link_to itinerary.name, itinerary %>
        </li>
      </ul>
    <% end %>
  <% end %>

</div>


<div class="controls">
  <%= link_to 'Edit member account', edit_user_path(@user), :class => 'btn btn-default' %>
  <%= link_to 'Delete member account', @user, :method => 'delete', :data => { :confirm => 'Are you sure you want to delete this account?' }, :class => 'btn btn-default' %>
</div>
